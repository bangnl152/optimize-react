import { createAction } from "redux-actions";

export const updateOffers = createAction("UPDATE_OFFERS");
export const fetchOffers = createAction("FETCH_OFFERS");
export const createOffer = createAction("CREATE_OFFER");
